package dungeon;

/**
 * This enum represents all the possible directions in which a cell can have
 * neighbours or in which a player can move.
 */
public enum Direction {
  NORTH,
  EAST,
  WEST,
  SOUTH;
}
